SECRET_KEY=
ADMIN_EMAIL=
ADMIN_PASSWORD=
# Diretório persistente do SQLite. Em produção (Render) aponte para o
# mount path de um Render Disk (ex.: /var/data). Em dev deixe em branco
# para usar ./data no próprio repo.
DATA_DIR=
UPLOAD_FOLDER=
